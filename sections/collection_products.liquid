<div class="w-layout-blockcontainer container w-container"><div class="row row-between"><div class="col col-lg-4 col-sm-12"><div class="u-d-flex cc-vertical cc-gap-24"><h1 li-object="collection.title">{{ collection.title }}</h1><div class="u-d-flex cc-horizontal cc-left-center cc-gap-16"><div class="detail u-color-secondary">Sort</div><select id="sort-by" li-for:inside="option in collection.sort_options" class="input cc-select">
        {% for option in collection.sort_options %}                            
                            <option value="{{ option.value }}" {% if option.value == collection.sort_by %}selected{% elsif option.value == collection.default_sort_by %}selected{% endif %}>{{ option.name }}</option>
                            
                    {% endfor %}    </select>
</div></div></div><div class="col col-sm-12"><div class="product_list">{% for product in collection.products %}    <div li-for="product in collection.products">
                                    
                            <script src="{{ 'liquify_custom.js' | asset_url }}" type="text/javascript"></script>
<div li-element="product-variant-container" x-data="handleVariant({{ product | json | escape }}, {{ product.options_by_name | json | escape }}, {{ product.selected_or_first_available_variant | json | escape }})" x-init="$nextTick(() => { activateVariants() })" @setbundleitem.window="updateBundleItem($event.detail.product, $event.detail.quantity)"><a li-object:href="product.url" href="{{ product.url }}" class="product_item-link w-inline-block"><div class="u-p-relative"><div class="u-aspect-3x4"><img class="product_item-image" src="{{ product.featured_image | img_url: 'master' }}" alt="{{ product.featured_image.alt }}" sizes="(max-width: 767px) 100vw, (max-width: 991px) 728px, 940px" li-object:src="product.featured_image | img_url: 'master'" loading="lazy" li-object:alt="product.featured_image.alt" srcset="{{ 'visualelectric-2-p-500.jpeg' | asset_url }} 500w, {{ 'visualelectric-2-p-800.jpeg' | asset_url }} 800w, {{ 'visualelectric-2.jpeg' | asset_url }} 1024w">
{% if product.selected_or_first_available_variant.available != true %}    <img class="product_item-image cc-alt" src="product.images[1] | img_url: 'master'" alt="product.images[1].alt" li-if="product.selected_or_first_available_variant.available != true" sizes="(max-width: 767px) 100vw, (max-width: 991px) 728px, 940px" li-object:src="product.images[1] | img_url: 'master'" loading="lazy" li-object:alt="product.images[1].alt" srcset="{{ 'visualelectric-1-p-500.jpeg' | asset_url }} 500w, {{ 'visualelectric-1-p-800.jpeg' | asset_url }} 800w, {{ 'visualelectric-1.jpeg' | asset_url }} 1024w">
{% endif %}</div>{% if product.selected_or_first_available_variant.available != true %}    <div li-if="product.selected_or_first_available_variant.available != true" class="product_item-scrim u-p-absolute cc-full u-d-flex cc-vertical cc-center-center u-events-none">
    <div class="u-p-absolute u-color-primary-invert">OUT OF STOCK</div>    </div>
{% endif %}</div>{% if product.selected_or_first_available_variant.available == true %}    <div li-if="product.selected_or_first_available_variant.available == true">
    <h2 li-object="product.title" class="detail">{{ product.title }}</h2><div class="u-d-flex cc-horizontal cc-gap-8">{% if product.compare_at_price_max > product.price %}    <div li-if="product.compare_at_price_max > product.price">
    <h3 li-object="product.compare_at_price | money" class="detail u-color-tertiary u-style-strikethrough">{{ product.compare_at_price | money }}</h3>    </div>
{% endif %}<h3 li-object="product.price | money" class="{% if product.compare_at_price > product.price %}u-color-sale{% else %}u-color-secondary{% endif %}">{{ product.price | money }}</h3></div>    </div>
{% endif %}{% if product.selected_or_first_available_variant.available != true %}    <div li-if="product.selected_or_first_available_variant.available != true">
    <h2 li-object="product.title" class="detail u-color-tertiary u-style-strikethrough">{{ product.title }}</h2><div class="u-d-flex cc-horizontal cc-gap-8">{% if product.compare_at_price_max > product.price %}    <div li-if="product.compare_at_price_max > product.price">
    <h3 li-object="product.compare_at_price | money" class="detail u-color-tertiary u-style-strikethrough">{{ product.compare_at_price | money }}</h3>    </div>
{% endif %}<h3 li-object="product.price | money" class="detail u-color-secondary u-style-strikethrough">{{ product.price | money }}</h3></div>    </div>
{% endif %}</a></div>
                            
                        </div>
{% endfor %}</div></div></div></div> {% schema %} {"tag":"section","name":"Collection Products","class":"section","presets":[{"name":"Collection Products","category":"Liquify","blocks":[]}],"enabled_on":{"templates":["collection"]}} {% endschema %}